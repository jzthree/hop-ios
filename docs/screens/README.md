# Screens

Captured against a demo fleet of scratch sessions (synthetic names,
taglines and output — created, staged, screenshotted and deleted by the
probe harness; see the STATUS change log for the recipe). No real
terminal content appears here by construction.

| Screen | What it shows |
| --- | --- |
| `wall-list.png` | The session list: taglines, live 3-line previews, attention-first ordering |
| `wall-tiles.png` | Tile view: live coloured miniatures of every screen |
| `hop-keyboard.png` | The hop keyboard: fixed height, mono face, terminal symbols one plane away |
| `disconnect-banner.png` | A disconnect tells its story: countdown, retry now, dimmed frozen content |
| `select-copy.png` | Hold-to-select with the Copy chip in its fixed slot |
